{{ 'custom-gallery.css' | asset_url | stylesheet_tag }}

<div class="container color-{{ section.settings.color_scheme }} gradient{% unless section.settings.background_style == 'none' and settings.text_boxes_border_thickness > 0 or settings.text_boxes_shadow_opacity > 0 %} background-{{ section.settings.background_style }}{% endunless %}">
  {%- if section.settings.heading != blank -%}
    <div class="row">
      <div class="col-12">
        <h2 class="my-4 text-center">{{ section.settings.heading }}</h2>
      </div>
    </div>
    {%- endif -%}
  {% if section.settings.sub_heading != blank %}
    <div class="row">
      <div class="col-12">
        <h3 class="mb-4 text-center">{{ section.settings.sub_heading }}</h3>
      </div>
    </div>
  {% endif %}

  <div class="gallery d-flex flex-wrap justify-content-center">
    {% for block in section.blocks %}
      {%- liquid
         if forloop.index == 1
          assign column_size = 'col-12 col-md-3' 
         elsif forloop.index == 2
          assign column_size = 'col-12 col-md-4' 
         elsif forloop.index == 3
          assign column_size = 'col-12 col-md-3' 
         elsif forloop.index == 4
          assign column_size = 'col-12 col-md-6' 
         elsif forloop.index == 5
          assign column_size = 'col-12 col-md-4' 
         elsif forloop.index == 6
          assign column_size = 'col-12 col-md-4' 
         elsif forloop.index == 7
          assign column_size = 'col-12 col-md-6' 
         else
          assign column_size = 'col-12 col-md-auto' 
         endif
      -%}
      <div class="gallery__column gallery__column--mobile-equalizer {{ column_size }}">
        {% if block.settings.image == blank %}
          {{ 'image' | placeholder_svg_tag: 'placeholder-svg placeholder' }}
        {% else %}
          <a class="gallery__link w-100 h-100" href="{% if block.settings.link != blank %} {{ block.settings.link }} {% endif %}">
            <div class="position-relative h-100">
              {% render 'responsive-image' with block.settings.image, alt: block.settings.image.alt class: 'gallery__image d-block w-100 h-100' %}
              <div class="gallery__caption">
                <h4 class="gallery__caption--title">{{ block.settings.title }}</h4>
                <p class="gallery__caption--description">{{ block.settings.description }}</p>
              </div>
            </div>
          </a>
        {% endif %}
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": {
    "en": "Designers Gallery"
  },
  "class": "custom-section--designers-gallery",
  "max_blocks": 7,
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": {
        "en": "Heading"
      },
      "default": "Heading Text"
    },
    {
      "type": "text",
      "id": "sub_heading",
      "label": {
        "en": "Sub Heading"
      },
      "default": "Sub-heading Text"
    }
  ],
  "blocks": [
    {
      "name": "Image",
      "type": "image",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": {
            "en": "Image"
          },
          "info": {
            "en": "1000 x 525px recommended"
          }
        },
        {
          "type": "url",
          "id": "link",
          "label": {
            "en": "Link"
          },
          "info": {
            "en": "Optional"
          }
        },
        {
          "type": "text",
          "id": "title",
          "label": {
            "en": "Heading"
          },
          "info": {
            "en": "Optional"
          }
        },
        {
          "type": "textarea",
          "id": "description",
          "label": {
            "en": "Description"
          },
          "info": {
            "en": "Optional"
          }
        }
      ]
    }
  ],
  "presets": [
    {
      "name": {
        "en": "Designers Gallery"
      },
      "category": {
        "en": "Image"
      },
      "blocks": [
        {
          "type": "image"
        },
        {
          "type": "image"
        },
        {
          "type": "image"
        }
      ]
    }
  ]
}
{% endschema %}