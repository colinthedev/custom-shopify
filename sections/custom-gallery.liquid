<!-- {{ 'section-image-gallery.min.css' | asset_url | stylesheet_tag }}

<section class="homepage-sections-wrapper homepage-sections-wrapper--white">
  <div class="container">
    {%- if section.settings.section_heading != blank -%}
      <div class="row">
        <div class="col-12">
          <h2 class="mb-4 mt-4 text-center">{{ section.settings.section_heading }}</h2>
        </div>
      </div>
    {%- endif -%}
    <div class="row image-bar justify-content-center">
      {% for block in section.blocks %}

        {% case section.blocks.size %}
          {% when 1 %}
            {%- assign column_size = 'col-sm-12 col-md-12 image-bar--single-image image-bar--single-column-mobile' -%}
          {% when 2 %}
            {%- assign column_size = ' col-12 col-sm-6 col-md-6' -%}
          {% when 3 %}
            {%- assign column_size = 'col-sm-12 col-md-4 image-bar--single-column-mobile' -%}
          {% else %}
            {%- assign column_size = 'col-sm-12 col-md-2 image-bar--single-column-mobile' -%}
        {% endcase %}

        <div class="grid__item mb-4 col-12 col-md-2" {{ block.shopify_attributes }}>
          <div>
            {% if block.settings.link != blank %}
              <a 
                href="{{ block.settings.link }}"
                aria-label="{{ block.settings.link | split: '/' | last | replace: '-', ' ' | capitalize }}"
              >
            {% else %}
              <div class="image-bar__item">
            {% endif %}
  
            <div class="image-bar__content">
  
              {% if block.settings.image == blank %}
                  {{ 'image' | placeholder_svg_tag: 'icon--placeholder' }}
              {% else %}
                {% render 'responsive-image' with block.settings.image, alt: block.settings.image.alt %}
              {% endif %}
            </div>
  
            {% if block.settings.link != blank %}
              </a>
            {% else %}
              </div>
            {% endif %}
            
		{% comment %}  
            {% if block.settings.title != '' and block.settings.link != blank %}
              <h3 class="type-subheading type-subheading--1 image-bar--title mb-0"><a href="{{ block.settings.link }}">{{ block.settings.title }}</a></h3>
            {% elsif block.settings.title != '' and block.settings.link == blank %}
              <h3 class="type-subheading type-subheading--1 image-bar--title mb-0">{{ block.settings.title }}</h3>
            {% endif %}
		{% endcomment %}  

            {% if block.settings.description != '' %}
              <div class="image-bar--description">{{ block.settings.description }}</div>
            {% endif %}
          </div>
        </div>

      {% endfor %}

      {% if section.blocks.size == 0 %}
        <div class="placeholder-noblocks">
          {{ 'general.onboarding.no_content' | t }}
        </div>
      {% endif %}

    </div>
  </div>

</section>

{% schema %}
{
  "name": {
    "en": "Designers Gallery"
  },
  "class": "homepage-section--image-gallery-wrapper custom-section--designers-gallery",
  "max_blocks": 12,
  "settings": [
    {
      "type": "text",
      "id": "section_heading",
      "label": {
        "en": "Section Heading"
      },
      "default": "Heading Text"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": {
        "en": "Image"
      },
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": {
            "en": "Image"
          },
          "info": {
            "en": "1000 x 525px recommended"
          }
        },
        {
          "type": "url",
          "id": "link",
          "label": {
            "en": "Link"
          },
          "info": {
            "en": "Optional"
          }
        },
        {
          "type": "text",
          "id": "title",
          "label": {
            "en": "Heading"
          },
          "info": {
            "en": "Optional"
          }
        },
        {
          "type": "richtext",
          "id": "description",
          "label": {
            "en": "Description"
          },
          "info": {
            "en": "Optional"
          }
        }
      ]
    }
  ],
  "presets": [
    {
      "name": {
        "en": "Designers Gallery"
      },
      "category": {
        "en": "Image"
      },
      "blocks": [
        {
          "type": "image"
        },
        {
          "type": "image"
        },
        {
          "type": "image"
        }
      ]
    }
  ],
  "templates": [
    "index",
    "collection",
    "list-collections",
    "blog",
    "article",
    "page",
    "password",
    "404",
    "cart",
    "search",
    "product"
  ]
}
{% endschema %} -->